import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WidgetDogComponent } from './widget-dog.component';

describe('WidgetDogComponent', () => {
  let component: WidgetDogComponent;
  let fixture: ComponentFixture<WidgetDogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WidgetDogComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WidgetDogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
